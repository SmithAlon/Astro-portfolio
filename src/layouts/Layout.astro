---
import '../styles/global.css'
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Astro Basics</title>
	</head>
	<body>
		<slot />
		<div class="background-pattern fixed inset-0 -z-10" role="presentation" aria-hidden="true"></div>

    <section class="decoration-layer" aria-hidden="true">
        
        <!-- Partículas decorativas -->
        <div class="particles" role="presentation">
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[10%] left-[15%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[25%] left-[85%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[45%] left-[75%] z-20"></div>
            <div class="particle fixed w-2 h-2 rounded-full bg-white opacity-20 top-[65%] left-[25%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[85%] left-[45%] z-20"></div>
            <div class="particle fixed w-2 h-2 rounded-full bg-white opacity-20 top-[15%] left-[55%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[35%] left-[95%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[55%] left-[5%] z-20"></div>
            <div class="particle fixed w-2 h-2 rounded-full bg-white opacity-20 top-[75%] left-[65%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[95%] left-[35%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[5%] left-[25%] z-20"></div>
            <div class="particle fixed w-2 h-2 rounded-full bg-white opacity-20 top-[40%] left-[15%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[60%] left-[85%] z-20"></div>
            <div class="particle fixed w-1 h-1 rounded-full bg-white opacity-30 top-[80%] left-[55%] z-20"></div>
            <div class="particle fixed w-2 h-2 rounded-full bg-white opacity-20 top-[20%] left-[45%] z-20"></div>
        </div>
    </section>
		<main class="relative min-h-[200vh]">
		</main>
	</body>
</html>

<style>
    /* Base Styles */
    html, body {
        margin: 0;
        background: #000000;
    }

    /* Background Pattern Layer */
    .background-pattern {
        background-color: #000000;
        opacity: 0.9;
        width: 100%;
        height: 100%;
    }

    /* Particle Animation System */
    .particle {
        animation: twinkle var(--twinkle-duration, 3s) infinite;
        animation-delay: var(--twinkle-delay, 0s);
    }

    /* Particle Animation Variants */
    .particle:nth-child(3n) { --twinkle-duration: 4s; --twinkle-delay: 0.5s; }
    .particle:nth-child(3n+1) { --twinkle-duration: 3.5s; --twinkle-delay: 1s; }
    .particle:nth-child(3n+2) { --twinkle-duration: 3s; --twinkle-delay: 1.5s; }
    .particle:nth-child(2n) { --twinkle-duration: 4.5s; --twinkle-delay: 2s; }
    .particle:nth-child(2n+1) { --twinkle-duration: 5s; --twinkle-delay: 2.5s; }

    /* Animation Keyframes */
    @keyframes twinkle {
        0%, 100% { opacity: 0.1; transform: scale(1); }
        50% { opacity: 0.6; transform: scale(1.5); }
    }

    @keyframes subtle-float {
        0%, 100% { transform: translateY(0px); }
        50% { transform: translateY(-10px); }
    }
</style>

<script src="https://cdn.jsdelivr.net/npm/gsap@3.13.0/dist/ScrollTrigger.min.js"></script>