<section class="presentacion flex flex-col items-center justify-center w-full min-h-screen p-4 text-center">
    <h2 class="text-3xl sm:text-4xl md:text-5xl lg:text-6xl font-semibold hollow-text max-w-[90%] sm:max-w-3xl">Alonso Smith</h2>
    <h1 class="text-xl sm:text-2xl md:text-3xl lg:text-4xl font-bold mt-2 text-[#F0D338] max-w-[90%] sm:max-w-2xl">Desarrollador Jr.</h1>
    <p class="mt-2 text-xs sm:text-sm max-w-[90%] sm:max-w-xl text-white">Y estudiante de Ingeniería en Desarrollo de Software</p>
</section>

<style>
    .hollow-text {
        color: transparent;
        -webkit-text-stroke: 1px rgb(240, 211, 47);
        text-stroke: 1px white;
    }
</style>

<script>
    import { gsap } from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';

    gsap.registerPlugin(ScrollTrigger);

    gsap.to('.presentacion', {
        scale: 0.5,
        autoAlpha: 0,
        scrollTrigger: {
            trigger: '.presentacion',
            start: 'top top',
            end: '+=500',
            scrub: true,
            pin: true
        }
    });
</script>