<main>
    <section class="presentacion flex flex-col items-center justify-center h-screen text-white px-4 text-center">
        <h2 class="text-5xl md:text-5xl lg:text-6xl font-semibold hollow-text max-w-3xl">Alonso Smith</h2>
        <h1 class="text-2xl md:text-3xl lg:text-4xl font-bold mt-2 text-[#F0D338] max-w-2xl">Desarrollador Jr.</h1>
        <p class="mt-2 text-sm md:text-sm lg:text-sm max-w-xl">Y estudiante de Ingeniería en Desarrollo de Software</p>
    </section>
</main>

<style>
    .hollow-text {
        color: transparent;
        -webkit-text-stroke: 1px rgb(240, 211, 47);
        text-stroke: 1px white;
    }
</style>

<script>
    import { gsap } from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';

    gsap.registerPlugin(ScrollTrigger);

    gsap.to('.presentacion', {
        scale: 0.5,
        autoAlpha: 0,
        scrollTrigger: {
            trigger: '.presentacion',
            start: 'top top',
            end: '+=500',
            scrub: true,
            pin: true
        }
    });
</script>